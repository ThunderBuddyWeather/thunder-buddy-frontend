import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    borderRadius: 4,
    marginTop: 20,
    minHeight: 50,
    paddingHorizontal: 20,
    paddingVertical: 10,
  },
});
